Pre-compiled on Windows 7.  May need to do this on Windows 2008 R2 in future.

On a build server such as TeamCity or TFS you may not have the AxImp.exe file that is a part of
the MSBUILD task to build the interop assemblies.  SmtpAgent.csproj is the dependent project.