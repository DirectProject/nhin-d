namespace Health.Direct.Config.Console.Command
{
    /// <summary>
    /// Convience class that keeps the access to these constant values in a single location with
    /// similar accessibility.
    /// </summary>
    internal static class Constants
    {
        internal const string CRLF = "\r\n";
        internal const string EntityStatusString = "New | Enabled | Disabled";
    }
}